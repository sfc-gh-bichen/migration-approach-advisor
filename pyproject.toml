[project]
name = "migration-approach-advisor"
version = "0.1.0"
description = "Recommends 2-3 ranked Snowflake migration approaches from customer code, meeting notes, and context"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]
